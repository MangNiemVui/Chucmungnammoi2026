Đặt các asset hình ảnh (từ file image.rar) vào thư mục này.

Ví dụ: bgr.jpg, background1.jpg, apricot-blossom.png, cloud*.png ...

Nếu bạn không dùng ảnh, giao diện vẫn chạy với nền gradient (fallback).
